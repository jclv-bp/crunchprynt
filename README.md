# Bluprynt Verified Issuer Registry — PoC

Implementation of the registry described in `spec.md`. Next.js 15 + Tailwind v4 + Prisma + SQLite.

## Setup
```
npm install
cp .env.example .env
npm run dev
```
